# --- secrets ----------------------------------------------------------------
.env
.env.*
!.env.example

# --- python -----------------------------------------------------------------
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.eggs/

# --- virtualenv / uv --------------------------------------------------------
.venv/
venv/

# --- tooling caches ---------------------------------------------------------
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
.coverage.*
htmlcov/
coverage.xml

# --- node / frontend --------------------------------------------------------
node_modules/
# pnpm is the only package manager — keep npm/yarn lockfiles out of the repo.
package-lock.json
yarn.lock

# --- editors / os -----------------------------------------------------------
.DS_Store
.idea/
.vscode/

# Runtime FMP parquet cache lives at ~/.cache/fmp_engine (outside the repo).

# Dead self-extracting installer (base64 blob). Reconstructable from engine/;
# never commit it. Kept out of the tree and out of ruff/large-file hooks.
install_engine.py
